package proto.pb;

import "compatibility.proto";

message ConnectInfo {
    optional string version = 1;
    optional CompatibilityItems compatibilityItems = 2;
    optional int32 proxyPort = 3;
}