This directory holds plugins (e.g. hardware-accelerated video decoder) for common.lib.
Each plugin is a dynamic library (.dll or .so), loaded at runtime by PluginManager class (netoptix_vms/common/src/plugins/pluginmanager.h)
