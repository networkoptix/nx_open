<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <parent>
        <artifactId>cpp</artifactId>
        <groupId>com.networkoptix.hdwitness</groupId>
        <version>2.5.0-SNAPSHOT</version>
        <relativePath>../cpp</relativePath>
    </parent>

    <groupId>com.networkoptix.hdwitness</groupId>
    <version>2.5.0-SNAPSHOT</version>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>nxtool</artifactId>
    <packaging>pom</packaging>
    <name>NX Server Configuration Tool</name>    
    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>build_variables</artifactId>
            <version>${project.version}</version>
            <type>pom</type>
            <scope>system</scope>
            <systemPath>${root.dir}/build_variables/pom.xml</systemPath>
        </dependency>
    </dependencies>   
    <properties>
        <product.title>${nxtool.name}</product.title>  
        <product.display.title>${nxtool.display.name}</product.display.title>
        <template>app</template>
        <!--Global libs-->      
        <qt.libs>qml quick core gui network</qt.libs>
        <qt.dir>${env.ENVIRONMENT}/qt542/${arch}</qt.dir>
        <global.libs/>
        <!--Specific os libs used-->
        <windows.oslibs>Ws2_32.lib</windows.oslibs>
        <windows.oslibs.release/>
        <windows.oslibs.debug/>
        <linux.oslibs/>
        <mac.oslibs/>
        <!--Global defines-->
        <global.defines/>
        <!--Specific os defines-->
        <windows.defines/>
        <linux.defines/>
        <mac.defines/>      
    </properties>    
</project>
