<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <parent>
        <artifactId>common_libs</artifactId>
        <groupId>com.networkoptix.hdwitness</groupId>
        <version>2.6.0-SNAPSHOT</version>
        <relativePath>..</relativePath>
    </parent>

    <groupId>com.networkoptix.hdwitness</groupId>
    <version>2.6.0-SNAPSHOT</version>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>udt</artifactId>
    <packaging>pom</packaging>
    <name>UDP-based Data Transfer Library</name>

    <properties>
        <!--Global libs-->
        <guid>${udt.guid}</guid>
        <template>lib</template>
        <qt.libs>core</qt.libs> <!-- TODO: cleanup compatibility_info.cpp -->
        <template>lib</template>
        <global.libs></global.libs>
        <ffmpeg.libs/>
        <!--Specific os libs used-->
        <windows.oslibs>-lws2_32</windows.oslibs>
        <windows.oslibs.release/>
        <windows.oslibs.debug/>
        <linux.oslibs/>
        <mac.oslibs>-lstdc++ -lpthread -lm</mac.oslibs>
        <android.oslibs/>
        <!--Global defines-->
        <global.defines/>
        <!--Specific os defines-->
        <windows.defines>_USING_V110_SDK71_</windows.defines>
        <linux.defines/>
        <mac.defines/>
        <android.defines/>
        <ios.defines/>
        <buildLib>sharedLib</buildLib>
    </properties>

    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>build_variables</artifactId>
            <version>2.6.0-SNAPSHOT</version>
            <type>pom</type>
            <scope>system</scope>
            <systemPath>${root.dir}/build_variables/pom.xml</systemPath>
        </dependency>
    </dependencies>
</project>
