This plugin provides access to directory with image files using Camera Integration API (mainly, by implementing nxcip::DtsArchiveReader interface).

Images from specified directory are provided as a stream of frames with specified duration (in milliseconds). Images are read in unspecified order
