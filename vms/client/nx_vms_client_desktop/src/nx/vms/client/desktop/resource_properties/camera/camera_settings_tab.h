#pragma once

namespace nx::vms::client::desktop {

enum class CameraSettingsTab
{
    general,
    recording,
    io,
    motion,
    fisheye,
    advanced,
    web,
    analytics,
    expert,
};

} // namespace nx::vms::client::desktop
