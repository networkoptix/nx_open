 <assembly>
    <id>${platform}-${arch}-${release.version}.${buildNumber}</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <fileSets>
        <fileSet>
            <directory>${root.dir}/wixsetup/wixsetup-full/${arch}/bin</directory>
            <outputDirectory>..</outputDirectory>
            <includes>
                <include>${finalName}.msi</include>
            </includes>		
        </fileSet>	
        <fileSet>
            <directory>${project.build.directory}/server</directory>
            <outputDirectory>..</outputDirectory>
            <includes>
                <include>update.json</include>
                <include>install.bat</include>
            </includes>		
        </fileSet>				
    </fileSets>
</assembly>