<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <parent>
        <artifactId>build_ut</artifactId>
        <groupId>com.networkoptix.hdwitness</groupId>
        <version>2.6.0-SNAPSHOT</version>
        <relativePath>../build_ut</relativePath>
    </parent>

    <groupId>com.networkoptix.hdwitness</groupId>
    <version>2.6.0-SNAPSHOT</version>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>common_ut</artifactId>
    <packaging>pom</packaging>
    <name>HD Witness Common Library Unit Tests</name>

    <properties>
        <guid>${common_ut.guid}</guid>
        <product.title>HD Witness Common Library Unit Tests</product.title>
        <root.dir>${basedir}/../..</root.dir>
        <!--Specify the project to test-->
        <master.project.directory>common</master.project.directory>
        <!--Global libs-->
        <qt.libs>core gui network xml xmlpatterns sql concurrent multimedia</qt.libs>
        <ffmpeg.libs>-lavcodec -lavdevice -lavfilter -lavformat -lavutil -lswscale -lswresample -lpostproc</ffmpeg.libs>
        <global.libs>-lcommon -lnx_utils -lnx_network -lnx_streaming -lonvif -ludt</global.libs>
        <!--Specific os libs used-->
        <windows.oslibs>Ws2_32.lib DbgHelp.lib sigar.lib -lIphlpapi -lwinmm Wldap32.lib</windows.oslibs>
        <linux.oslibs>-lmp3lame -lsigar -lfaac -lvpx -lvorbisenc -lvorbis</linux.oslibs>
        <linux.arm.oslibs>-lsigar</linux.arm.oslibs>
        <windows.oslibs.release></windows.oslibs.release>
        <windows.oslibs.debug></windows.oslibs.debug>
        <mac.oslibs>-lbz2 -framework IOKit -framework CoreServices -lsigar-universal64-macosx</mac.oslibs>
        <!--Global defines-->
        <global.defines></global.defines>
        <!--Specific os defines-->
        <windows.defines>EC2_LIB_API=__declspec(dllimport) _VARIADIC_MAX=10</windows.defines>
        <linux.defines/>
        <mac.defines/>
        <path>${env.PATH}</path>
        <rdep.packages>openssl quazip</rdep.packages>
    </properties>

    <dependencies>
       <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>nx_utils</artifactId>
            <version>${project.version}</version>
            <type>pom</type>
            <scope>system</scope>
            <systemPath>${root.dir}/common_libs/nx_utils/pom.xml</systemPath>
        </dependency>

        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>nx_network</artifactId>
            <version>${project.version}</version>
            <type>pom</type>
            <scope>system</scope>
            <systemPath>${root.dir}/common_libs/nx_network/pom.xml</systemPath>
        </dependency>
    </dependencies>

</project>
