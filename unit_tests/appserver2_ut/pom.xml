<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <!--
    Example pom for delegating maven goals to msbuild so it builds the "solution"

    A solution can be a group of projects, but here we only have one.
    It should also be possible to have one maven project per solution project,
    and have a parent pom to deletegate the build.
  -->
    <parent>
        <artifactId>build_ut</artifactId>
        <groupId>com.networkoptix.hdwitness</groupId>
        <version>2.6.0-SNAPSHOT</version>
        <relativePath>../build_ut</relativePath>
    </parent>

    <groupId>com.networkoptix.hdwitness</groupId>
    <version>2.6.0-SNAPSHOT</version>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>appserver2_ut</artifactId>
    <packaging>pom</packaging>
    <name>appserver2 unit tests</name>

    <properties>
        <guid>${appserver2.guid}</guid>
        <product.title>appserver2 unit tests</product.title>
        <root.dir>${basedir}/../..</root.dir>
        <!--Specify the project to test-->      
        <master.project.directory>appserver2</master.project.directory>
        <!--Global libs-->
        <qt.libs>core network xml xmlpatterns sql concurrent multimedia</qt.libs>
        <ffmpeg.libs>-lavcodec -lavdevice -lavfilter -lavformat -lavutil -lswscale -lswresample -lpostproc</ffmpeg.libs>
        <global.libs>-lmediaserver_core -lcommon -lappserver2 -lonvif -lquazip -lnxemail</global.libs>
        <!--Specific os libs used-->
        <windows.oslibs>Ws2_32.lib DbgHelp.lib sigar.lib -lIphlpapi -lwinmm Wldap32.lib Crypt32.lib</windows.oslibs>
        <linux.oslibs>-lmp3lame -lsigar -lfaac -lvpx -lvorbisenc -lvorbis -ldl -lldap</linux.oslibs>
        <linux.arm.oslibs>-llber -lsasl2 -lsigar</linux.arm.oslibs>
        <windows.oslibs.release></windows.oslibs.release>
        <windows.oslibs.debug></windows.oslibs.debug>
        <mac.oslibs>-lbz2 -framework IOKit -framework CoreServices -lsigar-universal64-macosx</mac.oslibs>
        <!--Global defines-->
        <global.defines></global.defines>
        <!--Specific os defines-->
        <windows.defines>EC2_LIB_API=__declspec(dllimport) _VARIADIC_MAX=10</windows.defines>
        <linux.defines/>
        <mac.defines/>
        <path>${env.PATH}</path>
        <rdep.packages>openssl</rdep.packages>
    </properties>
    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>appserver2</artifactId>
            <version>${project.version}</version>
            <type>pom</type>
            <scope>system</scope>
            <systemPath>${root.dir}/appserver2/pom.xml</systemPath>
        </dependency>
    </dependencies>    
</project>
