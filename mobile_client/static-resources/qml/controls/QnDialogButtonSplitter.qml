import QtQuick 2.4

import com.networkoptix.qml 1.0

Rectangle {
    implicitHeight: dp(32)
    implicitWidth: dp(1)
    anchors.verticalCenter: parent ? parent.verticalCenter : undefined
    color: QnTheme.dialogButtonSplitter
}
