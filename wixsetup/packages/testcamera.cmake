set(artifact.name.testcamera ${artifact.name.testcamera})
set(testcamera_files
    "${ffmpeg_libraries}"
    "${qt_common_libraries}"
    "${nx_libraries}"
    "${bin_source_dir}/${testcamera.binary.name}"
)