{
    "version" : "${release.version}.${buildNumber}",
    "platform" : "${platform}",
    "arch" : "${arch}",
    "modification" : "${modification}",
    "client" : true,
    "minimalVersion" : "${release.version}.0"
}