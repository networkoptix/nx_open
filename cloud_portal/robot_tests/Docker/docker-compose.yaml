version: '3'
services:
  webapp:
    image: mediaserver
    ports:
      - "7001:7001"
    network_mode: "bridge"
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
      - ./video:/dokk
      - ./data/:/opt/networkoptix/mediaserver/var
      - ~/develop/devtools/util/:/scripts
    tmpfs:
      - /run
      - /run/lock
