customization: '$CUSTOMIZATION'

host: '$CLOUD_PORTAL_HOST'

cloud_database:
    host: '$DB_HOST'
    port: '$DB_PORT'
    username: '$DB_USER'
    password: '$DB_PASSWORD'
    database: '$DB_NAME'

cloud_db:
    url: 'http://$CLOUD_DB_HOST:$CLOUD_DB_PORT/cdb'

cloud_portal:
    url: 'https://$CLOUD_PORTAL_HOST'

cloud_gateway:
    url: 'http://$VMS_GATEWAY_HOST:$VMS_GATEWAY_PORT/gateway'

smtp:
    host: 'mail.smtp2go.com'
    user: 'smtpsender911'
    password: 'CfxGalPkaMIe'
    port: 587
    tls: true

admins:
    -
        - 'Admins'
        - 'cloud-portal-admins@networkoptix.com'

queue_name: '$INSTANCE_NAME'

max_retries: 10
