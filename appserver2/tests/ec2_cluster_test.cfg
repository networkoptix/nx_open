[ClusterTest]
serverList=Server1,Server2,Server3
# The test case list field can have multiple test case file separated by ',' .
# Each file serves as a test case group, the test case in a group will be issued
# in parallel fashion in different thread. Each group will be tested one by one
testCaseList=tc1.ini
serverStatusSyncTimeout=2
# A simple cluster that only has 2 servers
[Server1]
serverAddress=10.1.5.129
port=7001

[Server2]
serverAddress=10.1.5.108
port=7001