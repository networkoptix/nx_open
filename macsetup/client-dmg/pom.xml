<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<!--
    Example pom for delegating maven goals to msbuild so it builds the "solution"
 
    A solution can be a group of projects, but here we only have one.
    It should also be possible to have one maven project per solution project, 
    and have a parent pom to deletegate the build.
  -->
	<parent>
		<artifactId>macsetup</artifactId>
		<groupId>com.networkoptix.hdwitness</groupId>
		<version>2.6.0-SNAPSHOT</version>
		<relativePath>..</relativePath>
	</parent>

	<groupId>com.networkoptix.hdwitness</groupId>
	<version>2.6.0-SNAPSHOT</version>
	<modelVersion>4.0.0</modelVersion>
	<artifactId>client-dmg</artifactId>
	<packaging>pom</packaging>
	<name>Client Mac Package</name>

    <properties>
        <!-- Change it to point relatively to the root-->
        <root.dir>${basedir}/../..</root.dir>
    </properties>

    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>client.bin</artifactId>
            <version>${project.version}</version>
            <type>pom</type> 
            <scope>system</scope>
            <systemPath>${root.dir}/client.bin/pom.xml</systemPath>            
        </dependency>        
    </dependencies>		
	
	<build>
		<directory>${arch}</directory>
		<outputDirectory>${arch}</outputDirectory>	
	</build>
</project>