<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <!--
    Example pom for delegating maven goals to msbuild so it builds the "solution"
 
    A solution can be a group of projects, but here we only have one.
    It should also be possible to have one maven project per solution project, 
    and have a parent pom to deletegate the build.
  -->
    <parent>
        <artifactId>cpp</artifactId>
        <groupId>com.networkoptix.hdwitness</groupId>
        <version>2.2.0-SNAPSHOT</version>
        <relativePath>../../cpp</relativePath>
    </parent>

    <groupId>com.networkoptix.hdwitness</groupId>
    <version>2.2.0-SNAPSHOT</version>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>client_lite</artifactId>
    <packaging>pom</packaging>
    <name>HD Witness Client</name>

    <properties>  
        <basedir>/..</basedir>
        <root.dir>${basedir}/../..</root.dir>
        <product.title>${client.name}_lite</product.title>
        <!--Global libs-->
        <qt.libs>core gui network opengl xml multimedia concurrent</qt.libs>
        <!-- ATTENTION!!! -lestbase should be the LATEST in the list!!! Required for Linux -->
        <global.libs>-lcommon -lavcodec -lavdevice -lavfilter -lavformat -lavutil -lswscale -lpostproc -lprotobuf -lFestival -lestools -leststring -lestbase</global.libs>
        <!--Specific os libs used-->
        <windows.oslibs>DbgHelp.lib -lws2_32 -lIphlpapi -lwinmm -lDxErr -ld3d9 -lopenal32 -lssleay32 -llibeay32 -lsigar</windows.oslibs>
        <windows.oslibs.release/>
        <windows.oslibs.debug/>
        <linux.oslibs>-lssl -lcrypto -lz -lopenal -lX11 -lXfixes -lGL -lGLU -ltermcap -lsigar -lfaac -lXss</linux.oslibs>
        <mac.oslibs>-lbz2 -lz -framework IOKit -framework CoreServices -framework OpenAL -ltermcap -lsigar-universal64-macosx</mac.oslibs>
        <!--Global defines-->
        <global.defines>CL_FORCE_LOGO LIGHT_CLIENT</global.defines>
        <!--Specific os defines-->
        <windows.defines/>
        <linux.defines/>
        <mac.defines/>
    </properties>   
    <build>
        <sourceDirectory>../src</sourceDirectory>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>2.6</version>
                <executions>
                    <execution>
                        <id>copy-client-lite-resources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}</outputDirectory>
                            <overwrite>true</overwrite>
                            <encoding>UTF-8</encoding>                            
                            <resources>                                       
                                <resource>
                                    <directory>${parent.customization.dir}/client</directory>
                                    <filtering>false</filtering>
                                    <excludes>
                                        <exclude>**/_unused/**</exclude>
                                    </excludes>
                                </resource>	   
                                <resource>
                                    <directory>${customization.dir}/client</directory>
                                    <filtering>false</filtering>
                                    <excludes>
                                        <exclude>**/_unused/**</exclude>
                                    </excludes>
                                </resource>	        	                                                             
                            </resources>              
                        </configuration>            
                    </execution>
                </executions>
            </plugin>			
        </plugins>
    </build>    
</project>
