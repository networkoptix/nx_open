namespace nx {

internal static class AppInfo {
    public const string displayProductName = "${company.name} ${display.product.name}";
    public const string customization = "{customization}";
}

} // namespace nx
