<Include>
    <?ifndef includeGuard?>
        <?define includeGuard="app_info.wxi"?>

        <?define productId = "${paxtonProductId}"?>
        <?define productUpgradeCode = "${paxtonProductUpgradeCode}"?>
        <?define productName = "${display.product.name} Paxton Plugin"?>
        <?define manufacturer = "${company.name}"?>
        <?define productVersion = "${release.version}.${buildNumber}"?>
        <?define customizationDir = "${customization.dir}"?>
        <?define libraryName = "${paxtonDriverOutputName}.dll"?>
        <?define binariesDirectory = "${bin_source_dir}"?>
     <?endif?>
</Include>
