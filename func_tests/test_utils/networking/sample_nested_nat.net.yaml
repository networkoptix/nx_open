# A--B
# |  |
# D--C
# A cannot reach C, B cannot reach D on IP level.
# If mediaservers are installed on all nodes and connected into one system,
# all of them communicate with each other despite incomplete IP connectivity.
networks:
  10.254.1.0/24:
    a:
    b:
      10.254.2.0/24:
        c:
        d:
          10.254.3.0/24:
            e:
default_gateways:
